import { BussinessErrorFilter } from './bussiness-error.filter';

describe('BussinessErrorFilter', () => {
  it('should be defined', () => {
    expect(new BussinessErrorFilter()).toBeDefined();
  });
});
